# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==13.0
python-multipart==0.0.9
pydantic-settings==2.4.0
jinja2==3.1.4

# ── Database ───────────────────────────────────────────────────────────────────
# PostgreSQL (production)
asyncpg==0.29.0
psycopg2-binary==2.9.9          # for sync ops / Alembic migrations

# SQLite (development fallback — zero setup)
aiosqlite==0.20.0

# ORM
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3                  # DB migrations

# Knowledge base
pyyaml==6.0.2

# ── STT — Faster-Whisper (large-v3) ───────────────────────────────────────────
faster-whisper==1.0.3

# ── TTS — Kokoro (local, free, high quality) ──────────────────────────────────
# Requires espeak-ng on Windows: https://github.com/espeak-ng/espeak-ng/releases
kokoro>=0.9.4
soundfile==0.12.1

# ── TTS — Edge-TTS (cloud, Urdu fallback) ─────────────────────────────────────
edge-tts==7.0.2

# ── LLM ────────────────────────────────────────────────────────────────────────
ollama==0.3.3
openai>=1.50.0                   # also used for Groq compatibility layer
httpx==0.27.2                    # for ElevenLabs + direct API calls

# ── Voice Activity Detection ───────────────────────────────────────────────────
silero-vad==5.1.0

# ── Audio processing ───────────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.1
pydub==0.25.1

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.0
loguru==0.7.2
aiofiles==24.1.0
